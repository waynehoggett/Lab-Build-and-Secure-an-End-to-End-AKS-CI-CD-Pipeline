{{- if and .Values.productService.secret.create .Values.productService.env.MONGODB_URI }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.productService.secret.name | default "product-secrets" }}
  labels:
    app: {{ .Values.productService.name }}
type: Opaque
stringData:
  mongodb-uri: {{ .Values.productService.env.MONGODB_URI | quote }}
{{- end }}
